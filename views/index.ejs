<html>
  <head>
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="/styles.css" />
    <link rel="stylesheet" href="/index.css" />
  </head>
  <body>
    <%- include('navbar'); -%>
    <div class="newMessageContainer">
      <a href="./new" class="newMessageLink">New Message</a>
    </div>
    <div class="allMessagesContainer">
      <% for (let i = 0; i < message.length; i++) { %>
      <div class="messageContainer">
        <span class="messageContent messageText"> <%= message[i].text %> </span>
        <span class="messageContent"> <%= message[i].user %> </span>
        <span class="messageContent"> <%= message[i].added %> </span>
        <a href="/message/<%= message[i].id %>" class="openMessageLink">Open</a>
      </div>
      <% } %>
    </div>
  </body>
</html>
